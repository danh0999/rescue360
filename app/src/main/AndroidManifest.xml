<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PRM392_project"
        tools:targetApi="31">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/api_key"/>
        <activity android:name=".ui.activities.HomeActivity"/>
        <activity android:name=".ui.activities.RegisterActivity"/>
        <activity android:name=".ui.activities.LoginActivity"/>
        <activity android:name=".ui.activities.UserManagementActivity"/>
        <activity android:name=".ui.activities.ServiceOrderActivity" />
        <activity android:name=".ui.activities.PaymentMethodActivity" />
        <activity android:name=".ui.activities.RequestListActivity"/>
        <activity android:name=".ui.activities.ProfileActivity"/>
        <activity android:name=".ui.activities.AdminDashboardActivity"/>
        <activity android:name=".ui.activities.ChatActivity"></activity>
        <activity
            android:name=".ui.activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>